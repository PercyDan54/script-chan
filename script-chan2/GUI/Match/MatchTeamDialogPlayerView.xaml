<UserControl x:Class="script_chan2.GUI.MatchTeamDialogPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:p="clr-namespace:script_chan2.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400" Height="40">
    <Grid Margin="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="PlayerName" VerticalAlignment="Center" FontSize="14" HorizontalAlignment="Left" Margin="5 0 0 0" Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" />
        <Button x:Name="SwitchPlayer" Grid.Column="1" Visibility="{Binding RoomOpenVisible}" Content="{x:Static p:Resources.MatchTeamDialogPlayerView_SwitchPlayerButtonText}" />
    </Grid>
</UserControl>
